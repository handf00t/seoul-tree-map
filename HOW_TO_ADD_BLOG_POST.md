# 📝 블로그 포스트 추가 가이드

블로그 시스템은 마크다운 파일 기반으로 작동합니다. 각 포스트는 독립적인 `.md` 파일로 관리되며, Front Matter를 통해 메타데이터를 설정합니다.

##  새 포스트 추가하기

### 1단계: 마크다운 파일 생성

`src/data/posts/` 디렉토리에 새 마크다운 파일을 생성합니다.

**파일명 규칙:** `YYYY-MM-DD-title-slug.md`

예시:
- `2025-01-25-winter-tree-care.md`
- `2025-02-01-spring-planting-guide.md`

### 2단계: Front Matter 작성

파일 맨 위에 `---`로 감싼 Front Matter를 추가합니다:

```markdown
---
id: "6"
title: "겨울철 나무 관리법"
excerpt: "추운 겨울, 나무를 건강하게 지키는 방법을 알아봅니다."
category: "guide"
tags: ["겨울", "관리", "가이드"]
author: "박나무"
avatar: "/logo.svg"
publishedAt: "2025-01-25"
coverImage: "/blog/winter-care.jpg"
readTime: 5
featured: false
---

# 겨울철 나무 관리법

본문 내용을 여기에 작성합니다...
```

### 3단계: 본문 작성

Front Matter 아래에 마크다운 형식으로 본문을 작성합니다:

```markdown
## 소제목

일반 텍스트 단락입니다.

### 작은 소제목

- 리스트 항목 1
- 리스트 항목 2
- 리스트 항목 3

---

구분선을 사용할 수 있습니다.
```

### 4단계: 파일 목록 업데이트

`src/utils/markdownLoader.js` 파일의 `loadAllPosts()` 함수에서 파일 목록에 새 파일을 추가합니다:

```javascript
const postFiles = [
  '2025-01-10-seoul-tree-map-intro.md',
  '2025-01-12-ecological-benefits.md',
  '2025-01-15-street-trees-story.md',
  '2025-01-18-feature-guide.md',
  '2025-01-20-january-update.md',
  '2025-01-25-winter-tree-care.md'  // 새 파일 추가
];
```

### 5단계: Public 폴더에 복사

터미널에서 다음 명령어를 실행하여 마크다운 파일을 public 폴더로 복사합니다:

```bash
cp src/data/posts/*.md public/posts/
```

### 6단계: 확인

개발 서버를 실행하고 브라우저에서 블로그 섹션을 확인합니다.

---

## 📋 Front Matter 필드 설명

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `id` | string | ✅ | 포스트 고유 ID |
| `title` | string | ✅ | 포스트 제목 |
| `excerpt` | string | ✅ | 포스트 요약 (카드에 표시) |
| `category` | string | ✅ | 카테고리 (guide, story, update, community) |
| `tags` | array | ✅ | 태그 목록 |
| `author` | string | ✅ | 작성자 이름 |
| `avatar` | string | ❌ | 작성자 아바타 이미지 경로 |
| `publishedAt` | string | ✅ | 발행일 (YYYY-MM-DD) |
| `coverImage` | string | ❌ | 커버 이미지 경로 |
| `readTime` | number | ✅ | 예상 읽기 시간 (분) |
| `featured` | boolean | ✅ | 추천 포스트 여부 |

## 🎨 카테고리

- **guide** - 가이드 (녹색 `#22C55E`)
- **story** - 이야기 (주황색 `#F59E0B`)
- **update** - 업데이트 (파란색 `#3B82F6`)
- **community** - 커뮤니티 (보라색 `#8B5CF6`)

## ✍️ 지원되는 마크다운 문법

- `# 제목` - H1 (큰 제목)
- `## 제목` - H2 (중간 제목)
- `### 제목` - H3 (작은 제목)
- `- 항목` - 리스트
- `---` - 구분선
- 일반 텍스트 단락

## 🖼️ 이미지 추가

1. 이미지 파일을 `public/blog/` 디렉토리에 저장
2. Front Matter에서 경로 지정:
   ```yaml
   coverImage: "/blog/your-image.jpg"
   ```

## 📁 파일 구조

```
seoul-tree-map/
├── src/
│   ├── data/
│   │   ├── posts/              # 마크다운 원본 파일
│   │   │   ├── 2025-01-10-seoul-tree-map-intro.md
│   │   │   ├── 2025-01-12-ecological-benefits.md
│   │   │   └── ...
│   │   └── blogPosts.js        # 카테고리 정의만 포함
│   ├── utils/
│   │   └── markdownLoader.js   # Front Matter 파싱 로직
│   └── components/
│       └── Blog/
│           ├── BlogView.jsx
│           ├── BlogCard.jsx
│           └── BlogDetail.jsx
└── public/
    └── posts/                  # 빌드용 마크다운 파일 (복사됨)
        ├── 2025-01-10-seoul-tree-map-intro.md
        └── ...
```

## 🔄 기존 포스트 수정

1. `src/data/posts/` 디렉토리의 해당 `.md` 파일 열기
2. Front Matter 또는 본문 수정
3. `cp src/data/posts/*.md public/posts/` 명령어로 복사
4. 브라우저 새로고침

## ✅ 체크리스트

새 포스트를 추가할 때:

- [ ] `src/data/posts/`에 `YYYY-MM-DD-title.md` 형식으로 파일 생성
- [ ] Front Matter 작성 (모든 필수 필드 포함)
- [ ] 본문 작성 (마크다운 형식)
- [ ] `src/utils/markdownLoader.js`의 `postFiles` 배열에 파일명 추가
- [ ] `cp src/data/posts/*.md public/posts/` 실행
- [ ] 브라우저에서 확인

## 💡 팁

- **ID 번호**: 기존 포스트의 가장 큰 ID에 +1
- **읽기 시간**: 약 200-300자/분 기준으로 계산
- **태그**: 2-4개 정도가 적당
- **요약**: 2-3줄 정도의 간단한 설명
- **커버 이미지**: 16:9 비율 권장
